context_version: 1
module: testkit
goals:
  - Deterministic chaos testing & property checks across components.
components:
  - "simulated clock, network (drop/dup/reorder), disk faults"
  - "linearizability checker for single-key histories"
scenarios:
  - name: partitions_and_restarts
    steps:
      - "partition leaders; force election; heal; validate history"
  - name: l0_storm
    steps:
      - "hammer writes; compaction throttling; tails within SLO?"
  - name: wal_powercut
    steps:
      - "kill during append; recover; model parity"
outputs:
  - "repro seeds"
  - "event trace JSON for viz replay"
